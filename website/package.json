{
  "name": "tara-website",
  "version": "1.0.0",
  "description": "TARA cotton clothing brand website",
  "scripts": {
    "dev": "eleventy --serve",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "npm run clean && eleventy",
    "test": "node --test tests/*.cjs test/performance-case-study.test.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "sharp": "^0.35.4"
  }
}
